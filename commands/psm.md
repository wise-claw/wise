---
description: ""
---

# WISE psm

`/wise:psm` is a compatibility alias for `/wise:project-session-manager`.

Read `skills/project-session-manager/SKILL.md`, follow its full instructions, and pass through the user's arguments:

```text
$ARGUMENTS
```
