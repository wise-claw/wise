## Install / Upgrade

```bash
npm i -g wise@{{VERSION}}
```

> **Package naming note:** the repo, plugin, and commands are branded **wise**, but the published npm package name remains [`wise`](https://www.npmjs.com/package/wise).
